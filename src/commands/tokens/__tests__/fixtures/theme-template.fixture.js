/* eslint-disable-next-line @typescript-eslint/no-var-requires */
const tokens = require('./tokens.fixture.json');

module.exports = {
  cardBackground: tokens.palettes.primary[100].hex,
  otherBackground: tokens.palettes.primary[90].hex,
};
